/**
 * Gimmick · Obsidian — Mobile Auth route (QA preview). At /obsidian-auth.
 * Login → Onboarding → First capture, navigable in sequence.
 */
import React from 'react';
import { ObsidianAuthScreen } from '@/components/obsidian';

export default function ObsidianAuthRoute() {
  return <ObsidianAuthScreen />;
}
